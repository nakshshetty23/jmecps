# lib

Reserved for third-party client singletons (database, storage, email,
payment gateway SDKs). Currently only holds `utils.ts` (shadcn/ui's `cn`
class-merging helper).

No external service clients are wired up yet — the site has no backend
integrations at this time.
