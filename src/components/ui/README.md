# components/ui

Atomic design primitives (Button, Input, Modal, etc.) shared across the app.

No components have been extracted here yet — existing pages still use
inline Tailwind markup. Populate this folder as repeated UI patterns are
factored out.
